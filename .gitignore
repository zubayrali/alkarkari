# deps
/node_modules

# generated content
.source

# staged locale (copies of locales/<locale>/ produced by `pnpm stage`;
# the committed trees live under /locales)
/content
/public
/.staged-locale
/.dev-locale-request
/.affine-dev-session.json

# test & build
/coverage
/.next/
/out
/build
*.tsbuildinfo

# build:all local mirror of the CI matrix+stitch (scripts/build-all.ts,
# scripts/stitch-deploy.ts) — per-locale builds + stitched site
/artifacts
/site

# misc
.DS_Store
*.pem
/.pnp
.pnp.js
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env
.env.affine
.env.publisher
.affine-publisher-state.json
.affine-publisher-state.json.before-*
.affine-publisher-runtime.json
.affine-publisher/
/affine/*
!/affine/publishing.config.json
!/affine/locales.config.json
/.scratch/
/var/

# others
.env*.local
.vercel
next-env.d.ts

# agent tooling / skill caches (local-only) — but project skills are shared
/.claude/*
!/.claude/skills/
/.agents/*
!/.agents/skills/
/.agents/skills/*
!/.agents/skills/affine-publisher/
!/.agents/skills/affine-publisher/**
!/.agents/skills/add-locale/
!/.agents/skills/add-locale/**
/.superpowers/
/skills-lock.json
/.recall/
